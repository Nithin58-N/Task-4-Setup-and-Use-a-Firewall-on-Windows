# Task 4 - Firewall Configuration

## Objective
Configure firewall rules to block Telnet (port 23) and allow SSH (port 22).

## Steps (Windows)
- Open Windows Firewall (wf.msc)
- Created inbound rule to block TCP port 23
- Verified rule in firewall console

## Outcome
Firewall successfully blocks Telnet traffic, improving system security.
